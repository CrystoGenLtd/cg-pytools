Run in the same way as growth rate script:
./vary_input.sh start increment number_of_jobs

Script will search for string "vary_param" and replace it with the value set here.
Currently set for floating point values, but script can be adapted.
